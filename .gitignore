node_modules
dist

# variáveis de ambiente locais — os .example ficam versionados
.env
.env.test

# server/.env.test é versionado de propósito: sem segredos, aponta
# para o banco local de teste, faz `pnpm test` funcionar após o clone
!server/.env.test

# artefatos do sistema operacional
.DS_Store
Thumbs.db

# logs
*.log

# rascunho de execução agêntica
.superpowers/
